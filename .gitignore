node_modules
dist
dist-ssr
*.local
.DS_Store
.env
.env.local
.vscode
.idea

# Office temp/lock files
~$*
*.tmp

# Data mentah / file lokal (gak perlu di repo)
*.xlsx
/npm

# Cloudflare Wrangler cache (R2/Pages CLI)
.wrangler/

# Artefak non-app (presentasi budget, dsb) — bukan bagian template
*.pptx
*.docx

# Backup tooling — config berisi service_role key & hasil dump (JANGAN ke repo)
ops/backup/backup.config.json
ops/backup/dumps/
ops/backup/backup.log

# Playwright MCP artifacts
.playwright-mcp/

# Kredensial lokal - JANGAN ke repo
.supabase-db-password.txt
.admin-credentials.txt
supabase/.temp/

# Rahasia tambahan utk brankas — JANGAN ke repo
ops/brankas/tambahan/
*.brankas
